import tensorflow as tf

g = tf.GraphDef()


g.ParseFromString(open("./models/frozen_model.pb", "rb").read())


[n for n in g.node if n.name.find(“input”) != -1] # same for output or any other node you want to make sure is ok


for n in g.node:
  if n.name.find("input") != 0:
    print n
  else:
    print "not there",n


[n for n in g.node if n.name.find("input") != -1]


for n in g.node:
	if n.name.find("image") != -1:
		result = n.name.find("image");
		print(n)



for n in g.node:
	if n.name.find("heatmap") != -1:
		result = n.name.find("heatmap");
		print(n)